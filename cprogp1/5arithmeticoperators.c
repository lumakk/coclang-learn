#include <stdio.h>
int main(){

   // + (addition)
   // - (subtraction)
   // * (multiplication)
   // / (division)
   // % (modulus) - remainder
   // ++ (increment)
   // -- (decrement)

    int x = 1; 
    int y = 2; 
    int sum;
    sum = x + y;

    printf("%d", sum);

    return 0;
}