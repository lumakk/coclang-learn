#include <stdio.h>
int main(){

    int age = 21; // integer (whole numbers)
    float gpa = 2.05; // floating pount (decimal numbers)
    char grade = 'A'; // single character
    char name[] = "Mac Lozano"; // character arrays

    printf("You are %d", age);

    return 0;
}