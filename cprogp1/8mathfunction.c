#include <stdio.h>
#include <math.h>


int main(){

   double A = sqrt(9);      // square root
   double B = pow(2, 4);    // exponents
   int C = round(3.67);     // round off
   int D = ceil(3.14);      // rounds up (increments by 1)
   int E = floor(3.99);     // rounds down (decrements by 1)
   double F = fabs(-68);    // absolute value of a number
   double G = log(3);       // logarithms
   double H = sin(45);      // sine
   double I = cos(45);      // cosine
   double J = tan(45);      // tangent

   printf("%lf", F);

    return 0;
}