#include <stdio.h>

int main(){

   int x = 10;

   x += 2; // x equals to 12
   x -= 3; // x equals to 9
   x *= 4; // x equals to 36
   x /= 5; // x equals to 7
   x %= 2; // x equals to 1
   

   printf("%d", x);

    return 0;
}