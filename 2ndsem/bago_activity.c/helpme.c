#include <stdio.h>

void insertionSort(int arr[]){
    for (int i = 0; i < cols; i++){

    }
    

}

int binearySearch(){
    while (low <= high)
    {
        int mid = low + (high - low) / 2;
        if(mid == target){
            return mid;
        } if (mid < target){
            low = mid + 1
        }
        
    }
    

}

int main(){

    // USER INPUT ROWS & COLS
    int rows, cols;
    printf("Input how many rows: ");
    scanf("%d", &rows);
    printf("Input how many columns: ");
    scanf("%d", &cols);

    // USER INPUT ELEMENTS

    // PRINT NON-SORTED ARRAY
    
    // SORT ARRAY

    // PRINT SORTED ARRAY

    // USER INPUT ELEMENT TO FIND

    // SEARCH
}